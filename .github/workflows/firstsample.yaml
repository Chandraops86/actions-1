name: My First Workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Welcome message
        run: echo "My first GitHub Actions Job"
      - name: Read file
        run: cat README.md
      - name: Create Directory
        run: mkdir actionsfolder
      - name: List files
        run: ls -lrt